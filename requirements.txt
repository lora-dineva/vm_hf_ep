python-dotenv>=1.0.0
requests>=2.28.0
opencv-python-headless>=4.8.0
# Optional: for --backend qwen-vl-utils (Qwen-style fps/resize sampling).
# Requires torch/torchvision (install separately if needed).
qwen-vl-utils>=0.0.10
